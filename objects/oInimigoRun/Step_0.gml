/// @description Insert description here
// You can write your code in this editor

scrInimigoMovimentacao(spd, range);


if (distance_to_object(oPlayer) < range)
{
	if(!alarm[0]) alarm[0] = 10;
}

// Inherit the parent event
event_inherited();

